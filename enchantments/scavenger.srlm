static {
    name: "Scavenger",
    levels: 1..5
};

amount = 0.3 * level;

description = {extra: [
    "Scavenge ",
    {text: String.uFormatted(amount), color: "gold", extra: [" Coins "]},
    "per monster level on kill."
], color: "gray"};

return {
    level,
    description
};