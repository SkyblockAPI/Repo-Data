static {
    name: "Thunderlord",
    levels: 1..7
};

amount = match (level) {
  >= 6 -> ((level * 10) - 10),
  else -> (level * 8),
}

description = {
    color: "gray",
    extra: [
        "Every ",
        { text: "3 ", color: "red" },
        "hits on a monster, strike ",
        { text: "lightning", color: "yellow" },
        ", dealing ",
        {text: String.uFormatted(amount), color: "lime", extra: ["% "]},
        "of the hit's damage."
    ]
};

return {
    level,
    description
};