# Farming for dummies

if ("farming_for_dummies_count" !in data) return;

call("stat_calc/put_bonus", {
  stat: "farming_fortune",
  amount: data.farming_for_dummies_count,
  color: "lime",
  bonus_stats,
  stats
});
