# Farming for dummies

if ("farming_for_dummies_count" !in data) return;

call("stat_calc/put_bonus", {
  stat: "farming_fortune",
  amount: Math.clamp(0, data.farming_for_dummies_count, 5),
  color: "lime",
  bonus_stats,
  stats
});
